kind = "web"
previewPath = "/"
title = "Mahlzeit+"
version = "1.0.0"
id = "artifacts/mahlzeit"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23322

[services.development]
run = "pnpm --filter @workspace/mahlzeit run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mahlzeit", "run", "build" ]
publicDir = "artifacts/mahlzeit/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23322"
BASE_PATH = "/"
